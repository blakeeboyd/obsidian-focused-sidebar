{
	"name": "obsidian-focused-sidebar",
	"version": "0.1.1",
	"description": "Toggle any sidebar section to full height",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"esbuild": "^0.24.0",
		"obsidian": "^1.12.2",
		"typescript": "^5.6.0"
	},
	"dependencies": {
		"monkey-around": "^3.0.0"
	}
}
